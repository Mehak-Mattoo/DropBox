/** @type {import('next').NextConfig} */
const nextConfig = {
  // You might not need the experimental.appDir setting for newer Next.js versions
  // as the App Router is now the default
};

module.exports = nextConfig;
